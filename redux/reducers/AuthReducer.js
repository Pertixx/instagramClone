const initialState = {
  id: "",
  name: "",
  password: "",
  token: "",
};

const AuthReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default AuthReducer;
